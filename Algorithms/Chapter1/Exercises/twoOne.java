// Write a Point2D client that takes an integer value 'N'
// from the command line, generates N random points in the 
// unit square, and computes the distance separating the
// closest pair of points
//

//Public class Point2D
//	Point2D(double x, double y)	create a point
// double x()				x coordinate
// double y()				y coordinate
// double r()				radius
// double theta()			angle
// double distTo(Point2D that)		Euclidean distance
// void draw()				
//

import java.util.math
import java.util.vector


public class twoOne
{
	public static int ClosestPair(int argN)
	{
		//Math.random() generates a 
		//number from 0.0 to 1.0
		//
		//There's a brute force method for doing
		//this problem but there's a more elaborate
		//algorithm with O(nlogn)
		Vector xVector = new Vector();
		Vector yVector = new Vector();
		for (int i = 0; i < N; i++)
		{
			x = math.Random();
			y = math.Random();
			Point2D(x, y);
			xVector.add(x);
			yVector.add(y);
		{
		for (


	}

	point2D(double x, double y){}
	double x(){}
	double y(){}
	double r(){}
	double theta(){}
	double distTo(Point2D that){}
	void draw(){}

}


